const Y = require('yjs'); const d1 = new Y.Doc(); const p = new Y.XmlElement('paragraph'); const t = new Y.XmlText(); t.insert(0, 'hello'); p.insert(0, [t]); d1.getXmlFragment('default').insert(0, [p]); console.log('Original:', d1.getXmlFragment('default').toString()); const tempDoc = new Y.Doc(); const p2 = new Y.XmlElement('paragraph'); const t2 = new Y.XmlText(); t2.insert(0, 'world'); p2.insert(0, [t2]); tempDoc.getXmlFragment('default').insert(0, [p2]); const d1frag = d1.getXmlFragment('default'); d1frag.delete(0, d1frag.length); d1frag.insert(0, tempDoc.getXmlFragment('default').toArray().map(e=>e.clone())); console.log('Restored:', d1frag.toString());  
