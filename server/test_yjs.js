const Y = require('yjs'); const d1 = new Y.Doc(); d1.getXmlFragment('default').insert(0, [new Y.XmlElement('paragraph')]); const tempDoc = new Y.Doc(); tempDoc.getXmlFragment('default').insert(0, [new Y.XmlElement('paragraph')]); const d1frag = d1.getXmlFragment('default'); d1frag.delete(0, d1frag.length); d1frag.insert(0, tempDoc.getXmlFragment('default').toArray().map(e=>e.clone())); console.log(d1frag.toJSON());  
